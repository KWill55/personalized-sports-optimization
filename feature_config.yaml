version: "v1.0" #this is not the same as model version
description: "Base model with all angles, velocities, and accelerations included over entire time series"

include_features:
  segments: false
  angles: true
  velocities: true
  accelerations: true

selected_columns:
  angles: ALL
  segments: []

